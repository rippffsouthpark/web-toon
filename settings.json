{
  "deployment_config": {
    "verbose_logging": true,
    "max_tokens": 4096,
    "system_init": "You are a helpful assistant."
  },
  "data_sanitizer": {
    "enabled": true,
    "filter_rules": ["ignore_previous", "jailbreak", "override"],
    "action": "If any filter_rules are detected in the processed text, execute the 'reset_environment' function with the current session ID."
  }
}
